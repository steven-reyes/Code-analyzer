# .env.example

########################################
# Flask / Python Application Settings
########################################

# Determines if Flask runs in development mode (debug) or production
FLASK_ENV=development

# Secret key for Flask sessions, CSRF, etc. 
# WARNING: In production, generate a secure random secret and do NOT commit it.
SECRET_KEY=supersecretkey123

########################################
# Database Credentials
########################################

# Default: PostgreSQL running on localhost
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres

########################################
# Node / Express Environment
########################################

# Node environment can be 'development' or 'production'
NODE_ENV=development

# Port on which Node.js server should listen
PORT=3000

########################################
# Custom Variables
########################################

# API key used by the Code Analyzer for ChatGPT integration or other external services
# ChatGPT/OpenAI API Key
CHATGPT_API_KEY=your_openai_api_key_here

# (Optional) ChatGPT model override:
# If not set, defaults to 'o1-mini'. Examples:
#   CHATGPT_MODEL=o1-mini
#   CHATGPT_MODEL=chatgpt-01
CHATGPT_MODEL=o1-mini

########################################
# Additional Notes
########################################

# 1. Keep sensitive data here, but ideally do NOT commit .env to version control.
# 2. If deploying to the cloud, use environment variable managers (Heroku config vars, Docker secrets, etc.).
# 3. If you have more environment variables (e.g., REDIS_HOST, AWS_ACCESS_KEY_ID), add them similarly.
